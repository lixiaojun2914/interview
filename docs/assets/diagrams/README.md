# 图表资源

图表按复杂度选择格式：

- 普通流程、时序与架构关系：在 Markdown 中使用 Mermaid。
- 精细排版或专业工具导出的静态图：保存为 SVG。
- 需要缩放、筛选、动画或交互的复杂图：保存为独立 HTML，并从主题文档链接或嵌入。

文件名统一使用小写英文与连字符，例如 `kubelet-pod-lifecycle.svg`。图表必须有清晰标题，主题文档中应补充文字说明，避免只放图不解释。
